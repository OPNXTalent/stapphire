import { NextResponse } from 'next/server';
import { supabaseAdmin } from '@/lib/supabaseAdmin';
import { generateHiringCriteria } from '@/lib/hiringCriteriaExtractor';
import { normalizeHiringCriteriaError } from '@/lib/hiringCriteriaError';
export const runtime='nodejs';
export async function POST(request:Request){try{const body=await request.json();const title=String(body.title||'').trim();const job_description=String(body.job_description||'').trim();if(!title||!job_description)return NextResponse.json({error:'Title and Job Description are required.'},{status:400});const {data,error}=await supabaseAdmin.from('phase1_requisitions').insert({title,job_description}).select('id').single();if(error)throw error;let hiringCriteriaStatus='ready';try{await generateHiringCriteria(data.id,job_description)}catch(extractionError){hiringCriteriaStatus='failed';console.error('Hiring Criteria extraction failed after requisition creation',{requisitionId:data.id,error:normalizeHiringCriteriaError(extractionError)})}return NextResponse.json({...data,hiring_criteria_status:hiringCriteriaStatus},{status:201})}catch(error){console.error('Requisition creation failed',{error:normalizeHiringCriteriaError(error)});return NextResponse.json({error:'Unable to create requisition. Confirm the Phase 1 SQL has been run.'},{status:500})}}
