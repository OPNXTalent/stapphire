import { CreateRequisitionForm } from '@/components/CreateRequisitionForm';
import { supabaseAdmin } from '@/lib/supabaseAdmin';

export const dynamic='force-dynamic';
export default async function Home(){const {data}=await supabaseAdmin.from('phase1_requisitions').select('id,title,created_at').is('archived_at',null).order('created_at',{ascending:false});return <><div className="page-heading"><span className="eyebrow">Hiring workspace</span><h1>Requisitions</h1><p className="muted">Create a role, then evaluate resumes against its Job Description.</p></div><section className="surface req-directory"><div className="section-heading"><div><span className="eyebrow">Active work</span><h2>Active requisitions</h2></div><span className="count-label">{data?.length||0} roles</span></div><div className="list">{data?.map(r=><a className="row" style={{gridTemplateColumns:'1fr auto'}} href={`/requisitions/${r.id}`} key={r.id}><strong>{r.title}</strong><span className="open-link">Open →</span></a>)}{!data?.length&&<p className="muted empty-copy">No active requisitions.</p>}</div></section><section id="new-requisition" className="create-section"><span className="eyebrow">Add a role</span><h2>Create requisition</h2><CreateRequisitionForm/></section></>}
